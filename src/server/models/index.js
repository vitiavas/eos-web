const BlockIndexState = require('./block-index-state.model');
const Storage = require('./storage.model');
const Rfid = require('./rfid.model');

module.exports = {
  BlockIndexState,
  Storage,
  Rfid
}